name: Sandbox
on: [push]
jobs:
    test-sandbox:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@master
              with:
                  repository: near/nearcore
            - uses: actions/cache@v2
              id: cache-sandbox4  # give it a name for checking the cache hit-or-not
              with:
                path: ./target/  # what we cache: the folder
                key: cache-sandbox4
                restore-keys: cache-sandbox4
            - run: make sandbox
              if: steps.cache-sandbox4.outputs.cache-hit != 'true'
            - name: 'Listing near'
              working-directory: ./
              run: ls target/
